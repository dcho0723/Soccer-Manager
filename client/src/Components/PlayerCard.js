import React from "react"

function PlayerCard({ dob, club, country, image, name, rating, position, pace, shot, pass, dribble, defence, physical, bench }) {


    return (
        <h1>{name}</h1>
    )
}

export default PlayerCard;